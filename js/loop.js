let num = Math.floor(Math.random() * 5);

console.log("最小の値は" + num + "です");

/*
while (num !== 0) {
  num = Math.floor(Math.random() * 5);

  console.log("現在の値は" + num + "です");
}
*/

// 1 + 2 + 3 ... + 99 + 100
for (let i = 1; i <= 100; i += 1) {
  // 処理を書く。
}
